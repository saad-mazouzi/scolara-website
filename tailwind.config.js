/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{js,jsx,ts,tsx}",
    ],
    theme: {
      extend: {
        colors: {
          logoYellow: "#FFC600", // Remplacez par le code HEX exact du jaune
          logoBlack: "#000000", // Noir comme dans le logo
        },
      },
    },
    plugins: [],
  };
  
  
  